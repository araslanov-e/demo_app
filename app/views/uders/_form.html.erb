<%= form_for(@uder) do |f| %>
  <% if @uder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@uder.errors.count, "error") %> prohibited this uder from being saved:</h2>

      <ul>
      <% @uder.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
